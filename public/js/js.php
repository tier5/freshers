<?php
/*
use App;
$subdomain=$_POST["subdomain"];
if (!empty($subdomain)) {
    $sub=Subdomain::where('subdomain','=',$subdomain)->first();
    if (count($sub)>0) {
        echo "<span class='status-not-available'>Subdomain Not Available.</span>";
    } else {
        echo "<span class='status-available'> Subdomain Available.</span>";
    }
}
*/

?>
